{
  "name": "cv-portfolio-netlify",
  "version": "2.1.0",
  "private": true,
  "description": "Portfolio CV na Netlify: automatyczne zakładki z folderu jobs i pobieranie CV jako PDF.",
  "scripts": {
    "build": "node scripts/build.js",
    "preview": "npx serve dist"
  },
  "engines": {
    "node": ">=20"
  }
}
